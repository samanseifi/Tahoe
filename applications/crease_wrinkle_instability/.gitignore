# Sweep driver output — regenerated by scripts/sweep_prestretch_phase_diagram.sh
sweep_runs/

# Per-run artifacts are also covered by the repo-root .gitignore
# (*.exo, *.out, *.log, *.echo.xml, *.valid.xml, *.rs*, err.txt) — listed
# here for clarity in case the app dir is consumed standalone.
*.exo
*.out
*.log
*.echo.xml
*.valid.xml
*.rs*
*.stdout
err.txt
SPOOLES.out
